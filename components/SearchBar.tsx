import React from "react";
import { Input } from "./ui/input";
import { Button } from "./ui/button";

const SearchBar = () => {
  return <Input className="w-full" type="text" placeholder="Search" />;
};

export default SearchBar;
